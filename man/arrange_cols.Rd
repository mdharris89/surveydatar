% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{arrange_cols}
\alias{arrange_cols}
\title{Arrange columns using flexible ordering strategies}
\usage{
arrange_cols(
  tab_result,
  .by = NULL,
  .order_fn = NULL,
  .sort = c("asc", "desc"),
  .partial = TRUE
)
}
\arguments{
\item{tab_result}{A tab_result object}

\item{.by}{Either a character vector of column labels (for explicit ordering),
or a row label/index to sort by cell values. If NULL, must provide .order_fn.}

\item{.order_fn}{Custom function that receives the first cell of each column
and returns a sort key. Alternative to .by.}

\item{.sort}{Sort direction: "asc" or "desc" (default "asc")}

\item{.partial}{Logical, if TRUE, unlisted columns remain in original order
after specified columns (default TRUE)}
}
\value{
Modified tab_result with reordered columns
}
\description{
Reorders columns using one of three strategies: by explicit label list, by cell
values in a row, or by a custom ordering function. Symmetric to arrange_rows().
}
\examples{
\dontrun{
# Explicit label order
result \%>\% arrange_cols(.by = c("Male", "Female", "Other"))

# Sort by values in a row
result \%>\% arrange_cols(.by = "Total", .sort = "desc")

# Custom ordering function
result \%>\% arrange_cols(.order_fn = function(cell) cell$base, .sort = "desc")
}
}
