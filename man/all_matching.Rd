% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builtins.R
\name{all_matching}
\alias{all_matching}
\title{Find all variables matching a pattern}
\usage{
all_matching(
  pattern,
  group_name,
  pattern_type = "fixed",
  label_mode = "full",
  drop_empty = TRUE
)
}
\arguments{
\item{pattern}{Single character string pattern to match against variable labels}

\item{group_name}{Question group name to search within}

\item{pattern_type}{Type of pattern matching: "fixed" (default), "regex", or "glob"}

\item{label_mode}{Label display mode (default "full")}

\item{drop_empty}{Whether to drop empty results if no matches found (default TRUE)}
}
\value{
Expression for use in tab() rows/cols (multi-column helper)
}
\description{
Finds all variables in a question group whose labels match a pattern
and returns each as a separate row. Unlike response_match which aggregates
matches, this returns individual variables that match the pattern.
}
\examples{
all_matching("Microsoft", "A1")
all_matching("Google|Microsoft", "A1", pattern_type = "regex")
all_matching("*Canva*", "A1", pattern_type = "glob")
}
