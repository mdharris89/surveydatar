% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab.R
\name{sort_tab}
\alias{sort_tab}
\title{Sort rows and/or columns of a tab result}
\usage{
sort_tab(tab_result, rows = NULL, cols = NULL, ...)
}
\arguments{
\item{tab_result}{A \strong{\code{tab_result}} object (the output of \code{\link[=tab]{tab()}}).}

\item{rows, }{cols  \strong{Sorting instructions} for rows or columns.  Either
\emph{\code{NULL}}   – leave the dimension unchanged – or \strong{one} of the
following:
\itemize{
\item \strong{Character scalar / vector}
\itemize{
\item \code{"label"} or \code{"row_label"} / \code{"col_label"} – alphabetical
(natural sort if \code{natural = TRUE} is later supplied).
\item \code{"original"} – restore the order in which \code{\link[=tab]{tab()}} created the
\item One or several \emph{data-column names} – value-based sorting (ties are
broken by the subsequent names in the vector).
}
\item \strong{List of control fields} – full syntax for advanced options.
\describe{
\item{\code{by}}{(character) What to sort by. Same values as above.}
\item{\code{order}}{\code{"asc"} or \code{"desc"} (default is \code{"asc"} for
\code{by = "label"} / \code{"original"}, \code{"desc"} otherwise).}
\item{\code{custom_order}}{Character vector giving an explicit order
for labels.  Anything not listed drops to the end.}
\item{\code{natural}}{Logical; if \code{TRUE} use natural / “human”
ordering for labels (e.g. “Q2” < “Q10”). Default \code{FALSE}.}
\item{\code{na.last}}{Logical; keep \code{NA} values at the end (\code{TRUE},
the default) or the front (\code{FALSE}).}
\item{\code{which}}{Subset of rows/columns to participate in the sort.
Can be numeric indices or labels/names.  All other
rows/columns keep their current relative order.}
}
}}

\item{...}{(reserved for future use; currently ignored).}
}
\value{
A \strong{\code{tab_result}} with the specified dimension(s) re-ordered.
All original attributes (arrays, base matrix, statistic descriptor,
etc.) are preserved so that subsequent calls to \code{\link[=sort_tab]{sort_tab()}},
significance tests, and printing continue to work.
}
\description{
Re-orders the rows and/or columns of a \strong{\code{tab_result}} produced by
\code{\link[=tab]{tab()}}, while always keeping the automatically-generated \strong{Base} and
summary (\strong{NET / Total / Avg}) rows / columns in their original
positions.
}
\examples{
tb <- tab(mydata, satisfaction, gender)

# 1.  Simple value-based: largest NET first
sort_tab(tb, rows = "NET")

# 2.  Alphabetical (natural) on rows; reverse-alphabetical on cols
sort_tab(tb,
         rows = list(by = "label", natural = TRUE),
         cols = list(by = "label", order = "desc"))

# 3.  Custom label order
sort_tab(tb,
         rows = list(
           by = "label",
           custom_order = c("Very satisfied", "Satisfied", "Neutral",
                            "Dissatisfied", "Very dissatisfied")))

# 4.  Sort only the first three data rows, leave the rest untouched
sort_tab(tb,
         rows = list(by = "label",
                     which = 1:3))

# 5.  Undo all sorting
sort_tab(tb, rows = "original", cols = "original")

}
