% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{define_rows}
\alias{define_rows}
\title{Define new row positions with custom matching rules}
\usage{
define_rows(tab_result, ..., .labels = NULL, .match = c("expr", "custom"))
}
\arguments{
\item{tab_result}{A tab_result object (cell-based)}

\item{...}{Matching criteria for new positions}

\item{.labels}{Character vector of labels for new positions (optional)}

\item{.match}{Matching strategy: "expr" (default) or "custom"}
}
\value{
Modified tab_result with new row definitions
}
\description{
Creates entirely new row positions using matcher functions. Each criterion
defines one position. Multiple cells can match each position, enabling
aggregated views.
}
\examples{
\dontrun{
# Define single-cell positions
result \%>\% define_rows(
  satisfaction == 5,
  satisfaction == 4,
  .labels = c("Very satisfied", "Satisfied")
)

# Define aggregated positions
result \%>\% define_rows(
  satisfaction >= 4,
  satisfaction <= 2,
  .labels = c("Top 2 box", "Bottom 2 box")
)

# Define with custom matchers
result \%>\% define_rows(
  function(cell) cell$base >= 100,
  function(cell) cell$specification$meta$priority == "high",
  .labels = c("High base", "High priority"),
  .match = "custom"
)
}
}
