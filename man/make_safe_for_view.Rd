% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surveymetadatafunctions.R
\name{make_safe_for_view}
\alias{make_safe_for_view}
\title{Make a data frame safe for viewing in any IDE}
\usage{
make_safe_for_view(x, cols = NULL)
}
\arguments{
\item{x}{A data frame, tibble, or dpdict.}

\item{cols}{Character vector of column names to sanitize.
If \code{NULL} (the default), all list-columns are targeted.}
}
\value{
A plain base \code{data.frame} with no list-columns in the
targeted set.
}
\description{
Converts list-columns to character strings so that interactive viewers
(Cursor, VS Code, RStudio) display a stable rectangular table without
column explosion or errors.
}
\details{
This is a \strong{display helper only}. Do not use the output for analysis or
as input to \code{\link{update_dat_from_dpdict}}.
}
\examples{
dpdict <- create_dict(get_minimal_labelled_test_dat())
dpdict |> make_safe_for_view() |> View()

# Equivalent workarounds for Cursor users:
# View(as.data.frame(dpdict))
# utils::View(dpdict)
}
