% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{as.data.frame.tab_result}
\alias{as.data.frame.tab_result}
\title{Methods to materialize cell-based tab_result objects into data.frame.
Convert cell-based tab_result to data.frame}
\usage{
\method{as.data.frame}{tab_result}(
  x,
  row.names = NULL,
  optional = FALSE,
  include_metadata = FALSE,
  show_base = NULL,
  label_mode = NULL,
  ...
)
}
\arguments{
\item{x}{A tab_result object (cell-based)}

\item{row.names}{Optional row names (usually NULL)}

\item{optional}{Unused (for S3 method compatibility)}

\item{include_metadata}{Attach full cell objects as attribute (default FALSE)}

\item{show_base}{Whether to display Base row/column showing sample sizes (default NULL, uses value from tab_result)}

\item{label_mode}{How to display labels: "smart" (suffix for multi-item, full for single-item),
"full" (complete variable label), or "suffix" (extract suffix after separator).
If NULL, uses value stored in tab_result object (default "smart").}

\item{...}{Additional arguments}
}
\value{
A data.frame with values from the layout grid
}
\description{
Materializes the cell collection into a standard data.frame by reading
values from cells according to the layout grid arrangement.
}
\examples{
\dontrun{
result <- tab2(data, satisfaction, gender)
df <- as.data.frame(result)

# Override label mode at display time
df_full <- as.data.frame(result, label_mode = "full")
df_suffix <- as.data.frame(result, label_mode = "suffix")

# With metadata attached:
df <- as.data.frame(result, include_metadata = TRUE)
cell <- attr(df, "cell_metadata")[[1, 1]]
}
}
