% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{dsl_subset_matcher}
\alias{dsl_subset_matcher}
\title{Create a DSL subset matcher}
\usage{
dsl_subset_matcher(query_dsl, data)
}
\arguments{
\item{query_dsl}{DSL expression to check for containment}

\item{data}{Data frame (for dsl_is_subset evaluation)}
}
\value{
Matcher function
}
\description{
Returns a function that checks if a DSL expression is a subset of
another using semantic matching.
}
\examples{
\dontrun{
matcher <- dsl_subset_matcher(quote(age > 18), data)
matcher(quote(age > 18 & gender == 1))  # TRUE (query is subset)
matcher(quote(gender == 1))              # FALSE (query not subset)
}
}
\keyword{internal}
