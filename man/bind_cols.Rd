% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conveniencefunctions.R
\name{bind_cols}
\alias{bind_cols}
\title{Bind columns while preserving metadata}
\usage{
bind_cols(...)
}
\arguments{
\item{...}{Data frames to combine. Each argument can either be a data frame,
a list that could be a data frame, or a list of data frames.
Inputs are \link[vctrs:theory-faq-recycling]{recycled} to the same length,
then matched by position.}
}
\value{
A data frame, as from dplyr::bind_cols, with metadata reapplied
}
\description{
Wrapper around dplyr::bind_cols that preserves per-column metadata, in
particular variable labels (attr(, "label")) and value labels (attr(, "labels")).
Any other simple attributes (except structural ones like names/class/dim) on
the input columns are also preserved on the corresponding output columns.
}
\details{
This function masks dplyr::bind_cols and is intended to be a drop-in
replacement. It accepts the same signature and passes all arguments through
to dplyr.
}
