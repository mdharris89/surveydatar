% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabtoreactable.R
\name{tab_to_reactable}
\alias{tab_to_reactable}
\title{Convert a surveydatar tab_result into a reactable-ready object}
\usage{
tab_to_reactable(
  tab_result,
  strip_summary_rows = FALSE,
  strip_summary_cols = FALSE,
  show_base = TRUE,
  color_mode = c("none", "heatmap", "top_n", "significance"),
  color_scope = c("column", "row", "table"),
  color_palette = NULL,
  color_midpoint = "mean",
  color_top_n = NULL,
  sig_comparison = NULL,
  sig_symbol = TRUE,
  freeze_headers = TRUE,
  freeze_first_column = TRUE,
  enable_sorting = TRUE,
  pagination = 20,
  searchable = FALSE,
  decimal_places = 1,
  font_family = "Consolas, Monaco, 'Courier New', Courier, monospace",
  ...
)
}
\arguments{
\item{tab_result}{A tab_result produced by surveydatar::tab()}

\item{strip_summary_rows}{Remove NET / Avg rows? (default FALSE)}

\item{strip_summary_cols}{Remove Total / NET columns? (default FALSE)}

\item{show_base}{Include base (n) row or column? (default TRUE)}

\item{color_mode}{Character: "none" (default), "heatmap", "top_n", or "significance"}

\item{color_scope}{Character: "column" (default), "row", or "table" (for heatmap and top_n modes)}

\item{color_palette}{Character vector of colors for gradients (NULL uses defaults)}

\item{color_midpoint}{Numeric value, "mean", or "median" for diverging color scale (default "mean")}

\item{color_top_n}{Integer: number of top values to highlight (required when color_mode = "top_n")}

\item{sig_comparison}{Character: name of significance comparison to use (auto-selects if only one exists)}

\item{sig_symbol}{Logical: whether to append significance symbols (default TRUE)}

\item{freeze_headers}{Logical: freeze table headers when scrolling (default TRUE)}

\item{freeze_first_column}{Logical: freeze first column when scrolling (default TRUE)}

\item{enable_sorting}{Logical: enable column sorting (default TRUE)}

\item{pagination}{Integer: rows per page, or FALSE to disable pagination (default 20)}

\item{searchable}{Logical: enable search/filter box (default FALSE)}

\item{decimal_places}{Integer: number of decimal places to display (default 1)}

\item{font_family}{Character: CSS font-family to use for the table (default "Consolas, Monaco, 'Courier New', Courier, monospace")}

\item{...}{Additional arguments (reserved for future use)}
}
\value{
An object of class "reactable_tab" containing:
\itemize{
\item data      – a data.frame with numeric values
\item metadata  – list with stat_info, base info, summary info, significance data
\item settings  – list with all display and color settings
}
}
\description{
Convert a surveydatar tab_result into a reactable-ready object
}
\details{
This function prepares a tab_result for display as an interactive HTML table using
the reactable package.
\subsection{Color Modes}{
\itemize{
\item \code{"none"}: No color coding (default)
\item \code{"heatmap"}: Gradient coloring based on values, with scope determining the scale
\item \code{"top_n"}: Highlight the top N values within the specified scope
\item \code{"significance"}: Color cells based on significance test results (requires significance data)
}
}

\subsection{Significance Display}{

When significance testing has been applied to the tab_result using \code{add_sig()} or
\code{add_sig_all()}, you can display results using:
\itemize{
\item \code{color_mode = "significance"}: Colors cells based on significance direction and level
\item \code{sig_symbol = TRUE}: Appends symbols to values
\itemize{
\item "↑*" for significantly above at 90\% (p < 0.10)
\item "↑**" for significantly above at 95\% (p < 0.05)
\item "↓*" for significantly below at 90\%
\item "↓**" for significantly below at 95\%
}
}

These two options can be used independently or combined.
}
}
\examples{
\dontrun{
# Basic usage
r_tab <- data \%>\% tab(q1, gender) \%>\% tab_to_reactable()
display_reactable(r_tab)

# With heatmap coloring
r_tab <- data \%>\%
  tab(satisfaction, region) \%>\%
  tab_to_reactable(color_mode = "heatmap", color_scope = "column")
display_reactable(r_tab)

# With significance testing
r_tab <- data \%>\%
  tab(satisfaction, region) \%>\%
  add_sig(versus = "North") \%>\%
  tab_to_reactable(color_mode = "significance", sig_symbol = TRUE)
display_reactable(r_tab)
}
}
